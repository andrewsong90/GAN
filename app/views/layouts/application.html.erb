<!DOCTYPE html>
<html>
<head>
  <title>GAN</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
	<% if user_signed_in? %>
		Logged in! 
		<strong><%= current_user.class.name %></strong> <%= current_user.fname %> <%= current_user.lname %>
	
		<%= link_to "Logout", destroy_user_session_path, method: :delete %>

		
		

	<% else %>
		<%= link_to "Sign Up", new_user_signup_path %> |
		<%= link_to "Log in", new_user_session_path %>
		<!-- TODO: Integrate with friend authorization-->
	<% end %>

<div class="container">
	<%= yield %>
</div>

</body>
</html>
